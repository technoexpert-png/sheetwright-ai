# Web
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # multipart file uploads
pydantic>=2.8
pydantic-settings>=2.4
itsdangerous>=2.2            # signed session cookies
email-validator>=2.0         # required by pydantic EmailStr

# Data
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
argon2-cffi>=23.1            # password hashing

# Spreadsheets. openpyxl handles .xlsx merged cells; .csv uses the stdlib.
# pandas is deliberately not used — see docs/decisions.md
openpyxl>=3.1
python-dateutil>=2.9

# Dev/test
pytest>=8.0
httpx>=0.27

# OPTIONAL — only for LLM_PROVIDER=anthropic. The stack runs on the mock
# provider without it, which is what keeps the test suite offline.
# anthropic>=0.40
